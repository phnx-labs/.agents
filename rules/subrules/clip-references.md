# Clip File References

A value shaped like `host:/absolute/path` is a real captured file or directory, not text. Read it locally when the host matches, otherwise fetch it over SSH; a sibling `.json` file may contain capture metadata.
