# Strict Testing

Place each test beside its source with nearby `testdata/`, exercise real services without mocks, and keep only tests that catch meaningful failures. Unit coverage is not enough; verify the real flow end to end.
