# agents-cli rules — single default preset
# `foundations` renders first: the principles every tactic references.
# Distilled corpus (2026-08-23): 23 subrules, goal + role + hard-lines.
presets:
  default:
    subrules:
      - foundations
      - research-discipline
      - parallel-teams
      - code-quality
      - testing-strict
      - truly-agentic-git-workflow
      - gh-merge-guard
      - no-pr-footer
      - operational
      - conventions
      - tech-stack
      - ui-work-discipline
      - plan-presentation
      - task-checklists
      - feed-status-posts
      - clip-references
      - demonstrate-on-ship
      - exhaust-self-serve
      - own-the-request
      - plan-artifacts
      - session-handoff
      - skill-restraint
      - stack
