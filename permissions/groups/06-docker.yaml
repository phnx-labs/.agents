  # Docker prefixes retained for readable translated policies. The blanket Bash
  # rule in 30-paths also allows privileged Docker commands at the permission layer.
  - "Bash(docker:*)"
  - "Bash(docker-compose:*)"
  - "Bash(podman:*)"
  - "Bash(colima:*)"
  - "Bash(buildx:*)"
