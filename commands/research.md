---
description: Alias for /research:research — answer a hard research question across DISTINCT engines blind to each other (Codex/web, Grok/X, Antigravity/Google, Perplexity/broad Deep Research, Claude/deep-read + reconcile), cross-check every claim (single-sourced = a lead, not a fact), and promote the cited result to the project's durable-artifacts home. For a hands-on PRODUCT exploration with driven screenshots + a clip, use /research:product.
argument-hint: "<research question or topic> [--depth quick|standard|deep] [--out <path>]"
---

**`/research` is an alias of `/research:research`.** One multi-engine research procedure,
defined once in the `research` plugin skill; this is the convenience short name. To explore a
**product** hands-on (drive it, screenshot each journey, prove claims visually), use
**`/research:product`** instead.

Invoke the `research:research` skill. Arguments: $ARGUMENTS
