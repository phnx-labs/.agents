---
description: Alias for /sessions:restore — re-open crash/reboot sessions as terminal windows
argument-hint: "[repo path or keyword to scope | 'all' | empty = auto-detect last crash]"
---

**`/restore` is an alias of `/sessions:restore`.** There is one restore procedure, defined
once in the `sessions` plugin skill; this is the convenience short name.

Invoke the `sessions:restore` skill. Arguments: $ARGUMENTS
