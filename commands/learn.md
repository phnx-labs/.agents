---
description: Post-session reflection that writes durable improvements forward — reflect on the session you just finished (or audit one skill/plugin/command across all past sessions) and route the lessons to their right home.
argument-hint: "[empty = current session | session-id | topic | <skill|plugin|command|workflow> to audit]"
---

Invoke the top-level **`learn`** skill (`skills/learn` — post-session reflection that
writes durable lessons forward). This is **not** `code:learn` (that one learns a codebase
into project `AGENTS.md`); the bare name is contested, so pick `skills/learn` here.
Arguments: $ARGUMENTS
