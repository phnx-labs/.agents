---
description: Debug end to end — clarify intent vs observed, find the spec gap, identify which agent/session caused a regression and how it slipped, confirm the root cause with blind independent agents, then close the loop (artifact, ticket, dispatch to the worker boxes). Routes to the swarm debug skill instead of solo file-reading.
---

Never debug solo — always route through the swarm debug skill.

Invoke the `swarm:debug` skill. Arguments: $ARGUMENTS
