---
description: Alias for /sessions:finish — drive the current task to fully delivered (verify E2E, docs, commit, PR, release, close the ticket). Never stops at a recap, blocker, or partial handoff.
---

**`/finish` is an alias of `/sessions:finish`.** There is one finish procedure, defined
once in the `sessions` plugin skill; this is the convenience short name.

Invoke the `sessions:finish` skill. Arguments: $ARGUMENTS
