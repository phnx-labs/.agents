---
description: Alias for /sessions:fork — fork this conversation into a new, independent session; the original is untouched.
---

**`/fork` is an alias of `/sessions:fork`.** There is one fork procedure,
defined once in the `sessions` plugin skill; this is the convenience short name.

Invoke the `sessions:fork` skill. Arguments: $ARGUMENTS
