---
description: Dispatch one task in this project — discover existing work, clarify the outcome, choose an executor, and own delivery
---

Invoke the `work:dispatch` skill for: $ARGUMENTS

Default to the current project. Preserve this lightweight one-task entry point;
use full planning when the task's uncertainty or design scope warrants it.
