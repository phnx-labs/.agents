---
description: Alias for /sessions:search — pull ranked, snippet-level context from prior sessions on a topic, without loading full transcripts.
---

**`/recall` is an alias of `/sessions:search`.** There is one recall procedure,
defined once in the `sessions` plugin skill; this is the convenience short name.

Invoke the `sessions:search` skill. Arguments: $ARGUMENTS
