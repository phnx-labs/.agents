---
description: Alias for /work:resume — pick a whole PROJECT's work back up. Best-effort detect the project from the CWD, reconstruct its in-flight work, then offload each item to a role=worker device (never the personal box).
---

**`/resume` is an alias of `/work:resume`.** There is one resume procedure, defined once in
the `work` plugin skill; this is the convenience short name.

Invoke the `work:resume` skill. Arguments: $ARGUMENTS
