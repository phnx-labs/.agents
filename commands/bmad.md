---
description: Turn a request into a BMAD-style, implementation-ready work package without starting the build
---

Use the global `bmad` skill for this request: $ARGUMENTS

Load and follow the skill before planning. The skill is the portable, canonical behavior;
this command is only its Claude-compatible accelerator. Keep the resulting package at the
approval boundary.
