---
description: Alias for /work:demo — demonstrate landed work: recover intent, exercise the shipped thing in its real environment, drive it signed in on real inputs, before/after side by side with a measured delta, deliver an analyzed report on your screen + the PR.
argument-hint: "[<ticket|PR|session-id|what was shipped> — empty demos what THIS session just landed]"
---

**`/demo` is an alias of `/work:demo`.** There is one demonstration procedure, defined
once in the `work` plugin skill; this is the convenience short name.

Invoke the `work:demo` skill. Arguments: $ARGUMENTS
