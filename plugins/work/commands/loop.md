---
description: Unattended general-purpose work drain across projects — code, browser, outreach; spread load; drive each item to landed, merging on green behind a non-author review.
---

Invoke the `work:loop` skill. Arguments: $ARGUMENTS
