---
description: Pick a whole PROJECT's work back up — best-effort auto-detect the project from the CWD (git repo + subdir → Linear), reconstruct its in-flight work (sessions, PRs, worktrees, tickets), then offload each item to a role=worker device, never the personal box.
---

Invoke the `work:resume` skill. Arguments: $ARGUMENTS
