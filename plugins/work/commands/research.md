---
description: Answer a hard research question across DISTINCT search engines, blind to each other, then synthesize one cited artifact. Codex runs aggressive web search, Grok mines X/Twitter community chatter (its privileged data), Antigravity works Google, Perplexity runs a broad browser-driven Deep Research report (the dedicated Deep-research mode, never Computer/Control-browser), and the Claude fleet deep-reads + reconciles. Cross-checks claims across engines — a single-sourced number is a lead to verify, not a fact — and promotes the result to docs/research/.
---

Invoke the `work:research` skill. Arguments: $ARGUMENTS
