---
description: Publish an HTML file to a shareable link. Public with an unfurling OG cover by default; pass --private for an unlisted, auto-expiring, no-preview link.
---

Invoke the `share` skill. Arguments: $ARGUMENTS
