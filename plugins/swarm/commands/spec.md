---
description: Specify a capability with swarm verification — reverse-engineer a plain-language spec of how the real code behaves (intent, behavior, sharp cases, what must not change) from the real code, then have independent agents describe it blind and check every claim against actual behavior.
---

Invoke the `swarm:spec` skill. Arguments: $ARGUMENTS
