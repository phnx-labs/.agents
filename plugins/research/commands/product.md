---
description: Explore a PRODUCT hands-on and prove every claim visually — don't just read about it. Compose research:research for the public intel (claimed features + sentiment), then sign up / install and DRIVE the real product through each user journey with agents browser/computer + secrets, screenshotting every step, recording a short clip of the headline flow, and putting landing-page CLAIMS next to what the product ACTUALLY did. Output is a favicon/logo-tagged, journey-diagrammed, screenshot-strip visual artifact — never one idle screenshot and a wall of text.
argument-hint: "<product(s) + what to explore> [--compare a,b,c] [--depth quick|standard|deep] [--record on|off]"
---

Invoke the `research:product` skill. Product / question: $ARGUMENTS
