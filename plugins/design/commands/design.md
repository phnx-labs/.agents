---
description: Design anything, keyless and offline-first. Routes a design intent to the right focused skill and renders self-contained HTML/SVG: design systems and tokens (design:system), screens and interactive flows (design:prototype), standalone graphics like logos/icons/OG cards (design:graphics), plus architecture/flow/ER diagrams, infographics, slide decks, a critique of an existing screen (design:critique), or anticipating a flow's dead-ends. True raster (photo, illustration) degrades to a spec plus editable placeholder when no backend is configured. Every skill loads design-core first (hierarchy, WCAG AA contrast, colorblind-safe palettes, brand-probe, the preflight for refining what already exists, the anti-tells catalog, precise copy, render/critique verification).
---

Invoke the `design` skill. Read `design-core.md` first, then route the intent to the right
focused skill (`design:system`, `design:graphics`, `design:prototype`, `design:critique`)
or mode file. Arguments: $ARGUMENTS
