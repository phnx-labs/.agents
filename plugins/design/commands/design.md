---
description: Design anything, keyless and offline-first. Routes a design intent to a mode and renders self-contained HTML/SVG: UI screens, clickable prototypes, design systems and tokens (including brand identity, BRAND.md), architecture/flow/ER diagrams, infographics, slide decks, vector assets (OG cards, logos, icons, posters), a critique of an existing screen, or anticipating a flow's dead-ends. True raster (photo, illustration) degrades to a spec plus editable placeholder when no backend is configured. Every mode loads design-core first (hierarchy, WCAG AA contrast, colorblind-safe palettes, brand-probe, live web inspiration, the anti-tells catalog, precise copy, render/critique verification).
---

Invoke the `design` skill. Read `design-core.md` first, then route the intent to the right mode file. Arguments: $ARGUMENTS
